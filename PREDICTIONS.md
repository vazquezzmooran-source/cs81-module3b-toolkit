# Function Predictions

## 1. double(n)
**Prediction**: When I call `double(5)`, I expect it to return `10` because the function multiplies the input by 2.

**Reasoning**: Multiplication by 2 should double any number value.

## 2. square(n)
**Prediction**: When I call `square(4)`, I expect it to return `16` because 4 × 4 = 16.

**Reasoning**: Squaring means multiplying a number by itself.

## 3. isEven(n)
**Prediction**: When I call `isEven(6)`, I expect it to return `true`, and `isEven(7)` should return `false`.

**Reasoning**: The modulo operator (%) will return 0 for even numbers and 1 for odd numbers when divided by 2.

## 4. isOdd(n)
**Prediction**: When I call `isOdd(7)`, I expect it to return `true`, and `isOdd(8)` should return `false`.

**Reasoning**: This is the opposite of isEven - odd numbers leave a remainder when divided by 2.

## 5. multiply(a, b)
**Prediction**: When I call `multiply(3, 7)`, I expect it to return `21`.

**Reasoning**: Basic multiplication operation should multiply the two input values together.
